apiVersion: v1
kind: ConfigMap
metadata:
  name: study-buddy-config
data:
  cognitoDomain: {{ .Values.config.cognitoDomain }}
  awsRegion: {{ .Values.config.awsRegion }}
  cognitoPoolId: {{ .Values.config.cognitoPoolId }}
  cognitoClientId: {{ .Values.config.cognitoClientId }}
