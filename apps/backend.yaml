apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: study-buddy-backend
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/andre-alves5/ap108b-k3s-gitops.git
    targetRevision: main
    path: charts/backend
  destination:
    server: https://kubernetes.default.svc
    namespace: study-buddy
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
