---
- name: Deploy Keda
  hosts: all
  become: true
  vars:
    ansible_python_interpreter: /usr/bin/python3
    kubeconfig_path: /etc/rancher/k3s/k3s.yaml

  tasks:
    - name: Add Keda Helm repo
      kubernetes.core.helm_repository:
        name: keda
        repo_url: https://kedacore.github.io/charts

    - name: Install KEDA CRDs
      kubernetes.core.k8s:
        src: https://github.com/kedacore/keda/releases/download/v2.14.0/keda-2.14.0-crds.yaml
        server_side_apply:
          field_manager: ansible
        kubeconfig: "{{ kubeconfig_path }}"

    - name: Install Keda via Helm
      kubernetes.core.helm:
        release_name: keda
        release_namespace: kube-system
        chart_ref: keda/keda
        create_namespace: true
        chart_version: 2.14.0
        kubeconfig: "{{ kubeconfig_path }}"
        skip_crds: true
        wait: true
        values:
          installCRDs: true
          resources:
            operator:
              limits:
                cpu: 500m
                memory: 256Mi
              requests:
                cpu: 100m
                memory: 128Mi
